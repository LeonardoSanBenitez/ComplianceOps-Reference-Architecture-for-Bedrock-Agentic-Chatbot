KNOWLEDGE_BASE_ID=          # required — terraform output bedrock_kb_id
AGENT_MODEL_ID=amazon.nova-micro-v1:0
CONV_LOG_BUCKET=            # required — terraform output conv_log_bucket_name
DYNAMODB_TABLE=             # required — terraform output dynamodb_sessions_table_name
REPORT_BUCKET=              # required — terraform output report_bucket_name
RETENTION_DAYS=30
CORS_ORIGIN=*
LOG_LEVEL=INFO
